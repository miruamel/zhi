# 2026-09-04-v0.1.4-published.md

**@brief** Fourteenth MANDAT re-injection: v0.1.4 tag push triggered `release` workflow `33840636854`; completed `success` at 05:32:59Z. Package published to npm as `@miruamel/zhi@0.1.4` (verified via `npm view`). GitHub Release created at target `main` with auto-extracted CHANGES.md notes. No token was used — OIDC Trusted Publisher (`id-token: write` + `--provenance`) handled auth end-to-end.

**@param** none

**@return** none

**@throw** none

**@since** 2026-09-04

**@see** https://github.com/miruamel/zhi/actions/runs/33840636854

---

## Context

The v0.1.4 tag was created locally at commit `bb9251a` (Prettier format on the version-0.1.4-promotion audit entry), which is the tip of `main`. The tag push triggered `.github/workflows/publish.yml` (`name: release`, `on: push: tags: v*.*.*`).

## What happened

| Step                                                    | Result                                           |
| ------------------------------------------------------- | ------------------------------------------------ |
| `actions/checkout@v4` (fetch-depth 0)                   | clean                                            |
| `Setup Node.js` 20.x + `Upgrade npm to latest` (npm 11) | clean                                            |
| `Setup Bun` 1.4.0                                       | clean                                            |
| `npm ci` (package-lock.json)                            | clean                                            |
| `bun run gate` (lint + format + typecheck + test)       | 365 pass / 0 fail / 726 expect() across 73 files |
| `bun run build` (TS → dist/)                            | clean                                            |
| `Setup Zig 0.16.0` (direct download)                    | clean                                            |
| `bun run native:build` (Zig WASM)                       | clean                                            |
| `npm publish --provenance --access public`              | **success** — OIDC Trusted Publisher             |
| `gh release create v0.1.4 --target main --verify-tag`   | **success**                                      |

## Verification

- `npm view @miruamel/zhi version` → `0.1.4`
- `npm view @miruamel/zhi dist-tags` → `{ latest: '0.1.4' }`
- `gh release view v0.1.4` → `isPrerelease: false`, `targetCommitish: main`, `publishedAt: 2026-09-04T05:32:56Z`
- Working tree: clean (nothing to commit)
- Audit log: 57 entries on disk, README index says 57, chronological
- Issues: 0 open. PRs: 0 open.

## Security note

No `NPM_TOKEN` was used. The publish used GitHub Actions OIDC federation (`id-token: write` permission + `npm publish --provenance`), which issues npmjs.com a short-lived Sigstore-signed token. The previously compromised token `npm_5xKx…` was never referenced in this workflow and remains revoked-or-should-be-rotated per §7.1 hygiene guidance.

## Deferred

- Dependabot config: still absent (P3, no alerts possible without it).
- v0.1.5 / next version: no work started; slow 0.x cadence per user preference.
