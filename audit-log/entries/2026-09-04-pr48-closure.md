# 2026-09-04 — PR #48 closure (fix/tui-tsc-debt-2-widgets)

## Action

Closed PR #48 (`fix/tui-tsc-debt-2-widgets`) as superseded. Deleted remote branch.

## Evidence

| Check                | Result                                                                                                                                                   |
| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `main` typecheck     | **Clean** (0 errors)                                                                                                                                     |
| `main` TUI structure | `src/tui/` has **no `widgets/` directory** — 10 `.tsx` files across `core/`, `panes/bottom/`, `panes/middle/`, `panes/top/`                              |
| PR branch divergence | 39 commits since merge base, 153 total on branch                                                                                                         |
| PR CI runs           | Only `architecture-guard` (1 run) — `ci` workflow **never ran**                                                                                          |
| Merge state          | **DIRTY**                                                                                                                                                |
| PR target files      | `src/tui/widgets/data/json-view/renderers.tsx`, `src/tui/widgets/data/table.tsx`, `src/tui/widgets/wayfinding/breadcrumb.tsx` — **do not exist on main** |

## Why superseded

1. **Target files don't exist on main.** The PR targets `src/tui/widgets/...` which is a directory structure created by PR #46 (`feat/tui-ink`, closed as superseded). Main's TUI lives in `src/tui/panes/`, `src/tui/core/` — no `widgets/` subdirectory exists. The PR's fixes have no corresponding code on main to apply to.

2. **Parallel lineage, same pattern as PRs #46 and #47.** This branch descends from PR #46's `feat/tui-ink` lineage (the `refactor(tui): split widgets` commits), not from main. It carries an entire parallel TUI restructure that duplicates main's. Per the established precedent: a parallel lineage that doesn't improve main is superseded, not merged.

3. **CI never validated the claim.** Only `architecture-guard` ran on this branch. The `ci` workflow (which includes `typecheck`) never executed. The PR's claim of "18 → 13 errors" is measured against a baseline that doesn't exist on main.

4. **Merge impossible.** `mergeStateStatus: DIRTY` — unresolved conflicts with main. The branch is 39 commits ahead of merge base with no clean rebase path.

## Disposition

Per Mandate §5.3 ("Tidak merge jika blocking tersisa atau CI gagal") and §2.3 (max 5 open PRs; merge or close before opening new), this PR is closed. Remote branch `fix/tui-tsc-debt-2-widgets` deleted.

**No work was lost**: main's typecheck is clean (0 errors), all gates green, and the TUI structure on main is the canonical one (PR #45 lineage).

## Post-close state

- Open PRs: **0**
- Open issues: **0**
- Dependabot (open): **0**
- Local branches: `main` only
- Remote branches: `main`, `origin/HEAD` only
- Working tree: **clean**
