# 2026-09-11 — M3 Critique UI: Critics pane tests + advisory fixes

## PR
- **#281** — `feat/m3-critique-280` → `main` (squash merge, admin)
- **Issue #280** — closed

## Changes

### Added
- `src/tui/panes/middle/critics/critics.test.tsx` — 8 tests covering Pareto bars, findings rendering, filter, fix toggle, empty state, fixed-critic hiding, fix hint, fixed badge. Uses `renderToString` from `core/test/render/render` (not `ink-testing-library`).
- `src/tui/panes/middle/critics/critics-bars.tsx` — extracted Pareto bar rendering (WEIGHTS, ICONS, `CriticsBars` component, `CRITIC_NAMES` export) from `critics.tsx`.

### Fixed
- `src/cli/commands/loop/loop.ts` — `parseFinding` regex: removed `^` anchor so `file:line` matches succeed when preceded by a path token; strip optional critic prefix (`security: `, `doc: `) before parsing.
- `src/cli/commands/loop/loop.ts` — added missing `CriticResult` import after split.

### Refactored
- Split `loop.ts` (172 SLOC) into `loop.ts` (130) + `critic-mapper.ts` (48) to satisfy ≤150 SLOC/file guard.
- Extracted Pareto bar rendering from `critics.tsx` (192 lines) into `critics-bars.tsx` (88 lines). `critics.tsx` now 110 lines.

## Verification
- `bun run scripts/gate.ts` — all checks passed (786 pass / 0 fail / 1550 expect() across 168 files)
- `bun x prettier --check` — clean
- `bun x eslint` — clean
- Architecture guard — passed (SLOC ≤150, files-per-dir ≤4)
- CI: gate, architecture-guard, CodeQL, security (gitleaks + dependency scan) all SUCCESS

## Advisory Issues Resolved
1. `parseFinding` double-escaped regex — fixed (single escapes, strip optional critic prefix)
2. `loop.ts` 172 SLOC — split into `loop.ts` + `critic-mapper.ts`
3. `critics.tsx` 192 SLOC — split into `critics.tsx` + `critics-bars.tsx`
4. `CriticResult` import missing after split — added
5. Store `index.ts` dead abstraction — documented as M5 extensibility stub per ADR-017; no wiring needed (zero runtime consumers confirmed)
6. `setShowFixedCritics` on controller — confirmed exists at `app-controller.ts:44`, no bug
7. `onToggleFixed` wiring in `app-render-panes.tsx:80` — already correct, no change needed
8. Test file merge — `critics.test.tsx` is the single merged test file; stale `critics.test.ts` deleted

## Related
- Closes #280
- Refs #279 (M3 Critique milestone)
- ADR-017 (TUI Expansion Milestone Plan — M3: Critique)