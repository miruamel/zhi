# 2026-08-29 — src/cli.ts boot entry (argv -> loop)

- **Type**: feat (entry point)
- **Action**: `src/cli.ts` — `main(argv)` parse goal + `--threshold=`, bangun `LoopContext`, jalankan `LoopDriver.run(buildHandlers(ctx, deps))` hingga DONE; cetak ringkasan JSON. `parseArgs` terpisah (testable). `import.meta.main` jalankan hanya bila dieksekusi langsung.
- **Offline stub**: `offlineDeps()` — `ingest/plan/generate/critique/ciGreen` deterministik (tanpa LLM nyata). `ponytail`: ganti dengan backend nyata (`route()` + client) bila env `ZHI_LLM_ENDPOINT` terisi. `router.ts` murni pemilih backend, belum ada pemanggilan LLM.
- **Verification**: `bun test` → 59 pass / 0 fail / 125 expect() / 13 file. Smoke nyata: `bun run src/cli.ts "build auth module"` → `{"goal":"build auth module","plan":"plan(build auth module)","code":"code(plan(build auth module))","score":0.9}`. SLOC cli.ts ~50. `src/` = 2 file + `tui/` (aman <5).
- **Impact**: agent Zhi kini invokable dari CLI; pipeline argv→INTAKE→…→DONE terbukti. Engine runnable tanpa god file.
- **Status**: resolved (lokal, belum push — network stall §2.11).
