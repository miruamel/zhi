# 2026-09-15-release-0.1.13.md

**@brief** Rilis `v0.1.13` dipromosikan di branch `release/v0.1.13` (tag `v0.1.13`) dengan bump metadata, tag `v0.1.13` lokal, dan entri audit ini. Belum dipush — publish ke GitHub Release + npm melalui `publish.yml` masih menunggu.

**@param** none

**@return** none

**@throw** none

| Tag `v0.1.13` | Dibuat lokal di branch `release/v0.1.13`. Belum dipush ke remote. |

**@see** https://github.com/miruamel/zhi/releases/tag/v0.1.13

---

## Context

Rilis PATCH `v0.1.13` menggabungkan 35 commit dari 18 PR sejak `v0.1.12` (tag `5bf92d9`). Semua perubahan backward-compatible — tidak ada fitur baru, bump PATCH sesuai SemVer.

## What happened

| Step | Result |
| --- | --- |
| Perbarui `package.json`, `package-lock.json`, `docs/configuration.md`, `src/cli/commands/init/init.ts`, `scripts/fix-since-tags.mjs` | Semua referensi `0.1.12` → `0.1.13` (kecuali `@since` historis). `package-lock.json` direkonstruksi dari `v0.1.12:package-lock.json` dengan mengganti hanya dua string `version` (line 3 dan line 9) — struktur indentasi dan urutan kunci identik, diff 2 baris. |
| `CHANGES.md` | Bagian `[0.1.13] - 2026-09-15` dengan 12 entri terinci (Added/Fixed); footer `[Unreleased]`, `[0.1.13]`, dan `[0.1.12]` diperbarui. Catatan #312/#314 ditambahkan ke bagian Fixed. |
| Tag `v0.1.13` | Dibuat lokal di branch `release/v0.1.13`. Belum dipush ke remote. |
| Audit entry | `audit-log/entries/2026-09-15-release-0.1.13.md` (entry ke-119). |

## Verification (lokal, branch `release/v0.1.13`)

- `node -e "JSON.parse(...)"` — `package-lock.json` valid, `lockfileVersion: 3`, `packages[""].version = 0.1.13`.
- `bun x prettier --check package-lock.json` — **lulus** (setelah rekonstruksi dari `v0.1.12`, prettier 3.9.6 kini menerima lockfile; sebelumnya `[warn]` karena indentasi root package tidak match).
- `git diff v0.1.12 -- package-lock.json` — 2 baris berubah (keduanya `version`).
- `git diff --stat` — 7 file berubah, semua referensi versi.
- Working tree bersih setelah commit + tag.

## Status publish

- Tag `v0.1.13`: lokal saja, belum di-push.
- npm `@miruamel/zhi@0.1.13`: belum terpublish.
- GitHub Release `v0.1.13`: belum dibuat.
- Langkah berikutnya: push branch `release/v0.1.13` + tag `v0.1.13`, buka PR, biarkan `publish.yml` create release + publish npm lewat OIDC Trusted Publishing.

## Security

Publish memakai GitHub Actions OIDC Trusted Publishing (`id-token: write` dan `npm publish --provenance`). Tidak ada token npm jangka panjang yang digunakan atau dicatat di audit ini.