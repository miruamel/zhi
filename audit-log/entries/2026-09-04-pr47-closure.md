# 2026-09-04 — PR #47 closure (fix/tui-tsc-debt)

## Action

Closed PR #47 (`fix/tui-tsc-debt`) as superseded. Left a detailed closing comment with evidence.

## Evidence

| Check               | Result                                                            |
| ------------------- | ----------------------------------------------------------------- |
| `main` typecheck    | **Clean** (0 errors)                                              |
| PR branch typecheck | **14+ errors** (ink-testing-library, Ink props, render callbacks) |
| PR CI runs          | Only `architecture-guard` (2 runs) — `ci` workflow **never ran**  |
| Merge state         | **DIRTY / CONFLICTING**                                           |
| Divergence          | 38 commits ahead of merge base, 70 behind main                    |

## Why superseded

1. **Debt already fixed on main.** `main` typecheck is clean. The PR's claim of "87% reduction (137 to 18)" is measured against a pre-PR-#45 baseline. PR #45 merged the ink TUI into main, and subsequent commits (keymap/state tests, toPatch coverage, format fixes) resolved the remaining type errors. Nothing left to sweep.

2. **Parallel lineage, not improvement.** The branch diverged before PR #45 was merged and carries its own TUI restructure (widgets, panes, integration subdirs) that duplicates main's. Per the PR #46 precedent: a parallel lineage that doesn't improve main is superseded, not merged.

3. **CI never validated the claim.** The PR's CI only ran `architecture-guard` (file-per-dir + SLOC + circular checks). The `ci` workflow — which includes `typecheck` — never executed on this branch. The 14 remaining type errors (ink-testing-library missing, Ink `onClick` prop violations, render callback signature mismatches) confirm the PR is not mergeable.

4. **Merge impossible.** `mergeStateStatus: DIRTY` — unresolved conflicts with main. Rebase fails on the first commit (`2caeca4`).

## Disposition

Per Mandate §5.3 ("Tidak merge jika blocking tersisa atau CI gagal") and §2.3 (max 5 open PRs), this PR is closed. The branch `fix/tui-tsc-debt` remains for reference; delete when convenient.

**No work was lost**: main already has the ink TUI (PR #45), the tsc-clean typecheck, and all the debt reduction this PR attempted to deliver.

**Superseded by** `2026-09-04-state-sync.md`. The branch `fix/tui-tsc-debt` was deleted locally + remotely after this entry was written (CHANGES.md line 113). Current state: 0 open PRs, 0 open issues, branch ledger contains only `main` + `origin/main` + `origin/HEAD`.
