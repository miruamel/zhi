# 2026-09-04-state-sync-6.md

## State sync keenam: PR #62 — cherry-pick 3 audit entries ke remote main

### Context

Remote `main` berada di commit `0d65a0e` (PR #60 branch head) yang sudah di-scrub via `git-filter-repo` (token di-redact), tetapi **hilang 3 audit entries** yang ada di local history:

1. `9980a0b` ci(architecture): add explicit permissions block
2. `10e6d71` docs: add merge-resolution-3 audit entry; fix branch protection check name in README
3. `fcb510e` + `4db0578` docs: P0 npm token leak — redact token, scrub history, update CHANGES.md

Force-push di-block oleh branch protection (`allow_force_pushes: false`). Solusi: cherry-pick 3 commit tersebut ke local `main` (reset ke `0d65a0e`), lalu push ke branch baru `fix/audit-log-sync`, buka PR #62.

### Tindakan

1. **Restore branch protection** via GitHub API (`PUT /branches/main/protection`):
   - `enforce_admins: true`
   - `allow_force_pushes: false`
   - `allow_deletions: false`
   - `required_linear_history: true`
   - `required_approving_review_count: 1`, `dismiss_stale_reviews: true`
   - 3 required status checks: Gate, Build, invariants
   - `restrictions: null`
   - Payload awal gagal (422) karena field `enforce_admins` harus boolean, bukan `{enabled: true}`. Diperbaiki.

2. **Reset local main** ke `0d65a0e` (remote state).

3. **Cherry-pick** 3 commit:
   - `9980a0b` → apply bersih (`c0ba2c3`)
   - `10e6d71` → conflict di `audit-log/README.md` (header count 70 vs 76). Di-resolve: ambil `76 file` + tambah entry `prettierignore-audit-log-exclusion`, update count jadi **71** (actual disk count).
   - `fcb510e` → conflict (history scrub, tidak applicable ke remote). Di-skip.
   - `4db0578` → apply bersih (`dad8e7a`)

4. **Push** ke `fix/audit-log-sync` (branch baru, push langsung diizinkan).

5. **PR #62** dibuka: `fix/audit-log-sync` → `main`, 3 commit, config-only + docs.

### Verifikasi

- `ls audit-log/entries/ | wc -l` = 71 (konsisten)
- `grep -c '2026-09-04-merge-resolution-3' audit-log/README.md` = 1 (terdaftar)
- `grep -c '2026-09-04-npm-token-leak' audit-log/README.md` = 1 (terdaftar)
- Branch protection: `enforce_admins: true`, `allow_force_pushes: false`, 3 checks aktif
- PR #62: `https://github.com/miruamel/zhi/pull/62`
- Tidak ada token yang bocor di working tree atau commit history

### Status

- Remote `main` masih di `0d65a0e` (menunggu merge PR #62)
- Branch protection aktif penuh: enforce_admins true, force-push false, 1 approval required, 3 status checks
- Audit log: 71 entries di disk, 71 terdaftar di README (konsisten)
- Token rotation masih pending (tugas P0 lanjutan)

### Risiko

Rendah. Config-only + documentation. Tanpa perubahan code atau dependency.
