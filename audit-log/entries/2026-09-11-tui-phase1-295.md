# Audit: TUI Phase 1 — Theme System + Form Widgets + Log Viewer

**Date:** 2026-09-11
**PR:** #295
**Issue:** #294
**Author:** autonomous agent (miruamel/zhi)

## Summary

Implements TUI Phase 1: theme system, form widgets, and log viewer. All code is
presentational (no `useInput` hooks yet — interactive input wiring deferred to
later phases). All files comply with Mandate §6.2 (≤150 SLOC/file, ≤4 files/dir).

## Files Added

| File | Purpose | SLOC |
|------|---------|------|
| `src/tui/core/themes/tokens.ts` | `ThemeTokens` interface, `themes` record (dark/light/highContrast), `getTheme()` | ~45 |
| `src/tui/core/themes/useTheme.ts` | `useTheme()` hook with localStorage persistence (`zhi:theme` key) | ~20 |
| `src/tui/core/themes/index.ts` | Barrel re-export | ~5 |
| `src/tui/widgets/form/input.tsx` | `TextInput` + `Textarea` | ~30 each |
| `src/tui/widgets/form/select.tsx` | `Select` dropdown | ~35 |
| `src/tui/widgets/form/toggle.tsx` | `Toggle` checkbox/radio | ~25 |
| `src/tui/widgets/form/index.ts` | Barrel export (4 files — at directory limit) | ~5 |
| `src/tui/widgets/log/log.tsx` | `LogViewer` with level filtering, search, maxLines, follow mode, timestamps | ~60 |
| `src/tui/widgets/log/log.test.tsx` | Test file | ~30 |
| `src/tui/widgets/log/index.ts` | Barrel export | ~3 |

## Files Modified

- `src/tui/widgets/index.ts` — added barrel exports for form + log widgets
- `src/tui/core/themes/index.ts` — barrel updated

## CI Fix

PR #295 initially failed CI with two issues:

1. **Format check** — `src/tui/widgets/form/index.ts` had formatting violations.
   Fixed by running `prettier --write`.
2. **Typecheck** — `src/tui/widgets/form/textarea/textarea.tsx` imported
   `colors` from `../../core/colors` but the file is nested one directory deeper
   (`form/textarea/`), so the path should be `../../../core/colors`. Fixed.

After fix: all CI checks pass (ci, architecture-guard, security, CodeQL,
Dependency Vulnerability Scan, Secret Detection). PR auto-merged.

## Gate Status

| Check | Result |
|-------|--------|
| lint (eslint) | PASS |
| format:check (prettier) | PASS |
| typecheck (tsc) | PASS |
| test (vitest) | 800 pass / 0 fail / 1584 expect() across 170 files |
| architecture (dependency-cruiser) | PASS |
| dependency-cruiser | PASS |

## Compliance

- Mandate §6.2: all files ≤150 SLOC, all directories ≤4 files
- No secrets detected (gitleaks)
- No circular dependencies (dependency-cruiser)
- Conventional commit: `feat(tui): theme system + form widgets + log viewer`