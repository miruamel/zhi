## Problem
M3 Critique milestone (v0.1.15) required comprehensive tests for the Critics pane and resolution of advisory issues from code review.

## Approach
- Wrote `src/tui/panes/middle/critics/critics.test.tsx` — 8 tests covering Pareto bars, findings rendering, filter, fix toggle, empty state, fixed-critic hiding, fix hint, and fixed badge.
- Split `loop.ts` (172 SLOC) into `loop.ts` (130 SLOC) + `critic-mapper.ts` (48 SLOC) to satisfy ≤150 SLOC/file guard.
- Fixed `parseFinding` regex: removed `^` anchor so `file:line` matches succeed when preceded by a path token; strip optional critic prefix (`security: `, `doc: `) before parsing.
- Added `CriticResult` import to `loop.ts` (was missing after split).
- Prettier-formatted all changed files.

## Verification
- `bun run scripts/gate.ts` — all checks passed (786 pass / 0 fail / 1550 expect() across 168 files)
- `bun x prettier --check` — clean
- `bun x eslint` — clean
- Architecture guard — passed

## Risks & Mitigations
- No breaking changes; all new props are optional with safe defaults.
- Rollback: revert PR (single commit).

## Related
- Closes #279 (M3 Critique milestone)
- ADR-017 (TUI Expansion Milestone Plan)