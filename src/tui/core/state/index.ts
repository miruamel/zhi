/** @brief AppState barrel — single source of truth for ink TUI state shape. @since 0.1.1 */
export type {
  CriticLine,
  EvalStage,
  EvalReport,
  DagStep,
  StageRecord,
  TimelineEntry,
  LogEntry,
  PrCiState,
  AppState,
} from "./state";
export { emptyState } from "./state";
