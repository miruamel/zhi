/** @brief ink glue for the core ShortcutRegistry (global + scoped hooks). @since 0.2.0 */
export {
  useGlobalShortcuts,
  useShortcut,
  ShortcutRegistry,
  matchShortcut,
  parseShortcut,
  type KeyCombo,
} from './shortcuts-registry';
